version: v1
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - DEFAULT
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
  rpc_allow_google_protobuf_empty_responses: true
breaking:
  use:
    - FILE
